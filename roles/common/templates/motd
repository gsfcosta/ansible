------------------------------------------------------------
		       SEJA BEM VINDO!
			  COMPASSO

HOSTNAME: "{{ ansible_hostname }}"
IP ADDRESS: "{{ ansible_default_ipv4 }}"
DISTRIBUICAO: "{{ ansible_distribution }}"

------------------------------------------------------------
